## Getting Started

This repository contains the website for the workshop.
To run the site locally, simply clone the repository and open `index.html`
in your browser.

For local development, open the repository e.g. in VS Code and use the
*Live Server* extension:
- Install the extension from the VS Code marketplace, if not already installed.
- Right-click `index.html` → **Open with Live Server**.

This will launch the website in your browser with automatic reload on file changes.